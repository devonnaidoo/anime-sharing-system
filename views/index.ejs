<% if (auth == false) { %>
<% include header %>
<% }%>

<% if (auth == true) { %>
<% include header-logged-in %>
<% }%>


<div class="container text-center">
    <h1 class="p-3">Home</h1>
    <% if(auth === true){%>
    <h3 class="p-1">Is Logged In</h1>
        <% } %>
        <% if(auth === false) { %>
        <h3 class="p-1">Is Not Logged In</h1>
            <% } %>
</div>