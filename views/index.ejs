<% include header %>

<div class="container text-center">
    <h1 class="p-5">Home</h1>
    <% if(isLoggedIn === true){%>
    <h1 class="p-5">Is Logged In</h1>
    <% } %>
    <% if(isLoggedIn === false) { %>
    <h1 class="p-5">Is Not Logged In</h1>
    <% } %>
</div>